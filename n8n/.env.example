# Standalone n8n secrets. Copy to .env in this folder.
#   copy .env.example .env   (Windows)

# A long random string. n8n encrypts stored credentials with this — keep it
# stable, or saved credentials become unreadable.
N8N_ENCRYPTION_KEY=CHANGE-ME-to-a-long-random-string
