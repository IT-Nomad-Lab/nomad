# fast path + client
httpx>=0.27
trafilatura>=1.9        # clean article-text extraction (no browser/LLM) — the fast path
cachetools>=5.3         # TTL result cache
fastapi>=0.110
uvicorn[standard]>=0.27
# search fallback (SearXNG is primary, via HTTP — no dep)
ddgs>=9.0               # DuckDuckGo fallback when SearXNG is unavailable
# heavy fallback (JS-heavy/blocked pages): headless Chromium + LLM extraction
scrapegraphai>=1.27,<2
